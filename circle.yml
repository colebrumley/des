machine:
  python:
    version: 3.5.1

dependencies:
  override:
    - python3 setup.py clean
    - python3 setup.py build 

test:
  override:
    - python3 setup.py test
  post:
    - pip3 install coverage
    - rm .coverage3; for m in cli watcher runner util; do coverage3 run -m des.$m -a; done
