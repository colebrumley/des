machine:
  services:
    - docker
  python:
    version: 3.5.1 

dependencies:
  override:
    - make clean
    - make init

test:
  override:
    - make test
  post:
    - bash <(curl -s https://codecov.io/bash)
