machine:
  python:
    version: 3.5.1

dependencies:
  override:
    - python3 setup.py clean
    - python3 setup.py build 

test:
  override:
    - python3 setup.py test
  post:
    - pip3 install coverage
    - rm .coverage; coverage3 run ./setup.py test
    - cp .coverage $CIRCLE_ARTIFACTS/
